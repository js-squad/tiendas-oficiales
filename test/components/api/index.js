import tap, { test } from 'tap';
import api from '../../../components/api/index.js';

var utils = {
  getQueryStr(){}
};

const apiObject = api(utils);

test('should add tests here', (t) => {
  t.plan(1);

  t.equal();
});