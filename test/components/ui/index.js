import tap, { test } from 'tap';
import ui from '../../../components/ui/index.js';

const utils = {
  ensureIsObject(){},
  ensureObjectHasProperty(){}
};

const Ui = ui(utils);

test('should add tests here', (t) => {

});